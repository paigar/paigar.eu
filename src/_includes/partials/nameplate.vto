<header class="site-header" role="banner">
  <div class="site-header-inner">
    <a href="/" class="site-header-mark">
      <span class="visually-hidden">Inicio · </span>
      <span class="site-header-mark-name">PAIGAR</span>
      <span class="site-header-mark-tagline">{{ site.subtitle }}</span>
    </a>
    <button type="button" class="site-header-toggle" aria-expanded="false" aria-controls="site-nav" aria-label="Abrir menú">
      <span class="site-header-toggle-bar"></span>
      <span class="site-header-toggle-bar"></span>
      <span class="site-header-toggle-bar"></span>
    </button>
    <nav id="site-nav" class="site-header-nav" aria-label="Navegación principal">
      <a href="/"{{ if url === '/' }} class="is-current" aria-current="page"{{ /if }}>Bitácora</a>
      <a href="/juegos/"{{ if url && url.startsWith('/juegos/') }} class="is-current" aria-current="page"{{ /if }}>Juegos</a>
      <a href="/herramientas/"{{ if url && url.startsWith('/herramientas/') }} class="is-current" aria-current="page"{{ /if }}>Herramientas</a>
      <a href="/relatos/"{{ if url && url.startsWith('/relatos/') }} class="is-current" aria-current="page"{{ /if }}>Relatos</a>
      <a href="/que-es-esto/"{{ if url === '/que-es-esto/' }} class="is-current" aria-current="page"{{ /if }}>Qué es esto</a>
    </nav>
  </div>
  <script>
    (function () {
      var btn = document.querySelector('.site-header-toggle');
      var nav = document.getElementById('site-nav');
      if (!btn || !nav) return;
      function setOpen(open) {
        btn.setAttribute('aria-expanded', open ? 'true' : 'false');
        btn.setAttribute('aria-label', open ? 'Cerrar menú' : 'Abrir menú');
      }
      btn.addEventListener('click', function () {
        setOpen(btn.getAttribute('aria-expanded') !== 'true');
      });
      nav.addEventListener('click', function (e) {
        if (e.target.tagName === 'A') setOpen(false);
      });
      document.addEventListener('keydown', function (e) {
        if (e.key === 'Escape' && btn.getAttribute('aria-expanded') === 'true') {
          setOpen(false);
          btn.focus();
        }
      });
    })();
  </script>
</header>
